# 🚀 Быстрый деплой за 5 минут

## Самый простой способ - Railway.app

### 1️⃣ Зарегистрируйтесь
Перейдите на https://railway.app и войдите через GitHub

### 2️⃣ Создайте новый проект
- Нажмите **"New Project"**
- Выберите **"Deploy from GitHub repo"**
- Выберите репозиторий `ProductPilot` (или как он называется)

### 3️⃣ Добавьте переменные окружения
В разделе **"Variables"** нажмите **"+ New Variable"** и добавьте:

```
GARMIN_EMAIL = ваш_email@example.com
GARMIN_PASSWORD = ваш_пароль
FLASK_SECRET_KEY = любая-случайная-строка
```

### 4️⃣ Готово! 🎉
Railway автоматически:
- Установит все зависимости
- Запустит приложение через gunicorn
- Даст вам URL типа: `https://road-to-sub5.up.railway.app`

---

## Альтернатива - Render.com (если готовы к холодному старту)

### 1️⃣ Зарегистрируйтесь
https://render.com - войдите через GitHub

### 2️⃣ Создайте Web Service
- **New +** → **Web Service**
- Подключите GitHub репозиторий
- Name: `road-to-sub5`
- Runtime: **Python 3**
- Build Command: `pip install -r requirements.txt`
- Start Command: `gunicorn app:app`

### 3️⃣ Добавьте переменные
В разделе **"Environment"**:
```
GARMIN_EMAIL
GARMIN_PASSWORD
FLASK_SECRET_KEY
```

### 4️⃣ Выберите план
**Free** (0$/месяц)

### 5️⃣ Create Web Service
Подождите 2-3 минуты - готово!

---

## 💰 Сравнение

| | Railway | Render |
|---|---------|--------|
| **Цена** | $5 кредиты/мес | Бесплатно |
| **Засыпает?** | ❌ НЕТ | ✅ ДА (15 мин) |
| **Скорость** | Очень быстро | Холодный старт 30 сек |
| **Сложность** | ⭐⭐ Легко | ⭐ Очень легко |

---

## 📝 Что нужно перед деплоем:

1. ✅ Закоммитьте все файлы в Git
2. ✅ Убедитесь что `.env` в `.gitignore`
3. ✅ Запушьте на GitHub

```bash
git add .
git commit -m "Add deployment configuration"
git push origin main
```

---

## 🔧 Если что-то пошло не так:

### Railway:
- Проверьте логи: нажмите на деплой → вкладка **"Deployments"** → **"View Logs"**

### Render:
- Проверьте логи: нажмите на сервис → вкладка **"Logs"**

### Типичные ошибки:
1. **Не установлен gunicorn** - добавьте в requirements.txt
2. **Неправильные переменные окружения** - проверьте имена
3. **База данных не создается** - Railway/Render автоматически создадут SQLite

---

## ✅ Проверка работы

После деплоя откройте URL и проверьте:
- [ ] Главная страница открывается
- [ ] Кнопка "Синхронизировать" работает
- [ ] Данные отображаются в таблицах

---

## 🎯 Мой выбор: Railway

**Почему:**
- Не засыпает (важно для регулярной синхронизации)
- Очень простой деплой
- $5 кредитов хватит на ~500 часов работы

**Railway подходит для вашего проекта идеально!** 👍
