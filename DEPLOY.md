# Инструкция по деплою Road to SUB5

## Вариант 1: Railway.app (Рекомендуется) 🚂

### Преимущества:
- ✅ $5 бесплатных кредитов в месяц (~500 часов работы)
- ✅ Не засыпает
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка переменных окружения

### Инструкция:

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   В разделе "Variables" добавьте:
   ```
   GARMIN_EMAIL=your_email@example.com
   GARMIN_PASSWORD=your_password
   FLASK_SECRET_KEY=your-secret-key-here
   PORT=5000
   ```

4. **Деплой происходит автоматически!**
   - Railway автоматически обнаружит Flask приложение
   - Установит зависимости из requirements.txt
   - Запустит через gunicorn

5. **Получите URL:**
   - После деплоя вы получите URL типа: `https://your-app.railway.app`

---

## Вариант 2: Render.com 🎨

### Преимущества:
- ✅ Полностью бесплатно (750 часов/месяц)
- ✅ HTTPS из коробки
- ✅ Простой интерфейс

### Минусы:
- ❌ Засыпает после 15 минут неактивности
- ❌ Холодный старт ~30 секунд

### Инструкция:

1. **Зарегистрируйтесь на Render:**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите ветку (main)

3. **Настройте сервис:**
   ```
   Name: road-to-sub5
   Runtime: Python 3
   Build Command: pip install -r requirements.txt
   Pre-Deploy Command: python init_db.py
   Start Command: gunicorn app:app
   ```

4. **Добавьте переменные окружения:**
   В разделе "Environment" добавьте:
   ```
   GARMIN_EMAIL=your_email@example.com
   GARMIN_PASSWORD=your_password
   FLASK_SECRET_KEY=your-secret-key-here
   ```

5. **Выберите план:**
   - Free (0$/месяц)

6. **Создайте сервис:**
   - Нажмите "Create Web Service"
   - Подождите 2-3 минуты

---

## Вариант 3: Fly.io 🚀

### Преимущества:
- ✅ 3 бесплатных VM
- ✅ Не засыпает
- ✅ Можно выбрать регион

### Инструкция:

1. **Установите Fly CLI:**
   ```bash
   # Windows (PowerShell)
   iwr https://fly.io/install.ps1 -useb | iex

   # Linux/Mac
   curl -L https://fly.io/install.sh | sh
   ```

2. **Войдите в аккаунт:**
   ```bash
   fly auth signup
   # или
   fly auth login
   ```

3. **Инициализируйте приложение:**
   ```bash
   fly launch
   ```

   Ответьте на вопросы:
   - App name: `road-to-sub5`
   - Region: выберите ближайший
   - Database: No (мы используем SQLite)

4. **Добавьте переменные окружения:**
   ```bash
   fly secrets set GARMIN_EMAIL="your_email@example.com"
   fly secrets set GARMIN_PASSWORD="your_password"
   fly secrets set FLASK_SECRET_KEY="your-secret-key"
   ```

5. **Деплой:**
   ```bash
   fly deploy
   ```

6. **Откройте приложение:**
   ```bash
   fly open
   ```

---

## Вариант 4: PythonAnywhere 🐍

### Преимущества:
- ✅ Полностью бесплатно
- ✅ Работает 24/7 БЕЗ засыпания
- ✅ Специально для Python

### Минусы:
- ❌ Нужно обновлять каждые 3 месяца
- ❌ Медленнее

### Инструкция:

1. **Зарегистрируйтесь:**
   - https://www.pythonanywhere.com
   - Бесплатный аккаунт "Beginner"

2. **Загрузите код:**
   - Dashboard → Files
   - Загрузите все файлы проекта

   Или через git:
   ```bash
   git clone https://github.com/your-username/road-to-sub5.git
   ```

3. **Создайте virtualenv:**
   - Dashboard → Consoles → Bash
   ```bash
   cd road-to-sub5
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

4. **Настройте Web App:**
   - Dashboard → Web
   - Add a new web app
   - Manual configuration → Python 3.10
   - Source code: `/home/username/road-to-sub5`
   - Working directory: `/home/username/road-to-sub5`
   - Virtualenv: `/home/username/road-to-sub5/venv`

5. **Настройте WSGI файл:**
   Отредактируйте `/var/www/username_pythonanywhere_com_wsgi.py`:
   ```python
   import sys
   path = '/home/username/road-to-sub5'
   if path not in sys.path:
       sys.path.append(path)

   from app import app as application
   ```

6. **Добавьте переменные окружения:**
   В том же WSGI файле в начале:
   ```python
   import os
   os.environ['GARMIN_EMAIL'] = 'your_email'
   os.environ['GARMIN_PASSWORD'] = 'your_password'
   ```

7. **Перезагрузите:**
   - Нажмите "Reload" в Dashboard → Web

---

## Вариант 5: Heroku (Платный, но популярный) 💳

**С ноября 2022 Heroku больше не предоставляет бесплатный план.**

Минимальная стоимость: $7/месяц

Если готовы платить:
```bash
heroku login
heroku create road-to-sub5
git push heroku main
heroku config:set GARMIN_EMAIL="your_email"
heroku config:set GARMIN_PASSWORD="your_password"
```

---

## 📊 Сравнение вариантов:

| Платформа | Цена | Засыпает? | Скорость | Сложность |
|-----------|------|-----------|----------|-----------|
| Railway | $5/мес кредиты | ❌ Нет | ⚡⚡⚡ | ⭐⭐ Легко |
| Render | Бесплатно | ✅ Да (15 мин) | ⚡⚡ | ⭐ Очень легко |
| Fly.io | Бесплатно | ❌ Нет | ⚡⚡⚡ | ⭐⭐⭐ Средне |
| PythonAnywhere | Бесплатно | ❌ Нет | ⚡ | ⭐⭐⭐ Средне |
| Replit | Бесплатно | ✅ Да | ⚡⚡ | ⭐ Очень легко |

---

## 🎯 Моя рекомендация:

### Для вашего проекта:

**1. Railway.app** - если нужна стабильная работа 24/7
**2. Render.com** - если готовы к холодному старту
**3. Fly.io** - если хотите больше контроля

---

## 🔒 Важно для безопасности:

**НЕ коммитьте .env файл в Git!**

Убедитесь что `.gitignore` содержит:
```
.env
*.db
training_data.db
__pycache__/
.venv/
```

---

## 🚀 Быстрый старт (Railway):

1. Зарегистрируйтесь на https://railway.app
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий
4. Добавьте переменные окружения
5. Готово! Приложение задеплоится автоматически

URL будет вида: `https://road-to-sub5-production.up.railway.app`

---

## 💡 Советы:

1. **База данных:**
   - SQLite будет работать, но данные могут теряться при рестарте
   - Для продакшена лучше использовать PostgreSQL (бесплатно на Railway/Render)

2. **Мониторинг:**
   - Railway и Render показывают логи в реальном времени
   - Можно настроить уведомления об ошибках

3. **Домен:**
   - Все платформы дают бесплатный поддомен
   - Можно подключить свой домен (обычно бесплатно)

4. **HTTPS:**
   - Все платформы предоставляют HTTPS автоматически

---

## 📞 Поддержка:

Если возникнут проблемы:
- Railway: https://railway.app/help
- Render: https://render.com/docs
- Fly.io: https://fly.io/docs
