# Быстрая настройка автодеплоя на Render

## ⚡ 3 минуты до автоматического деплоя

### Шаг 1: Включить Auto-Deploy

1. Зайдите на https://dashboard.render.com/
2. Откройте ваш сервис **road-to-sub5**
3. Перейдите на вкладку **Settings** (слева в меню)
4. Найдите раздел **"Build & Deploy"**
5. Найдите настройку **"Auto-Deploy"**
6. Переключите на **"Yes"**
7. Убедитесь, что выбрана ветка **"main"** (или ваша главная ветка)
8. Нажмите **"Save Changes"** (если есть кнопка)

### Шаг 2: Проверить webhook GitHub

Render должен был автоматически создать webhook в вашем GitHub репозитории.

**Проверить можно так:**

1. Зайдите на GitHub: https://github.com/Pavlovskyi-Andrii/sub5_project
2. Откройте **Settings** (репозитория, не профиля!)
3. Слева выберите **Webhooks**
4. Должен быть webhook с URL типа: `https://api.render.com/deploy/...`
5. Если его нет:
   - Вернитесь на Render
   - Settings → **Disconnect Repository** → **Connect Repository** снова

### Шаг 3: Тестируем автодеплой

```bash
# Сделайте тестовое изменение
echo "# Test auto-deploy" >> README.md

# Закоммитьте и запушьте
git add README.md
git commit -m "Test auto-deploy"
git push
```

**Что должно произойти:**

1. GitHub получит push
2. GitHub отправит webhook на Render
3. Render **автоматически** начнет новый деплой
4. Через 2-3 минуты новая версия будет live

**Как проверить:**

- Render Dashboard → Ваш сервис → вкладка **"Events"**
- Вы увидите новое событие: "Deploy triggered by push to main"
- Статус изменится: Building → Deploying → Live ✅

---

## 🎯 Теперь ваш workflow:

```bash
# 1. Делаете изменения в коде
vim app.py  # или любой другой файл

# 2. Коммитите
git add .
git commit -m "Добавил новую фичу"

# 3. Пушите
git push

# 4. Всё! Render автоматически задеплоит
# Через 2-3 минуты изменения будут на сервере
```

---

## 📊 Мониторинг деплоев

### Где смотреть статус

**Вариант 1: Render Dashboard**
- Dashboard → Ваш сервис → **Events**
- Видно все деплои с статусами

**Вариант 2: Email уведомления**
- Render отправляет email при успешном/неудачном деплое
- Настройки уведомлений: Settings → Notifications

**Вариант 3: GitHub Actions (бонус)**
- Можете добавить badge в README:
  ```markdown
  ![Deploy Status](https://img.shields.io/badge/deploy-success-brightgreen)
  ```

---

## ⚙️ Дополнительные настройки

### Деплой только определенных веток

**Settings → Build & Deploy:**
- **Branch:** выберите `main` или `production`
- Auto-Deploy будет срабатывать только для этой ветки
- Пуши в другие ветки (например, `dev`) не будут деплоиться

### PR Previews (платная фича)

На платном плане можно включить:
- Автоматические preview окружения для каждого Pull Request
- Тестируете изменения до мержа в main

### Rollback (откат)

Если что-то пошло не так:
1. Dashboard → Events
2. Найдите предыдущий успешный деплой
3. Нажмите **"Rollback to this version"**

---

## 🔔 Уведомления

### Настроить уведомления в Slack/Discord

1. Render Dashboard → Settings → **Notifications**
2. Добавьте webhook URL вашего Slack/Discord канала
3. Render будет отправлять уведомления о деплоях

---

## 🐛 Troubleshooting

### Автодеплой не работает

**Проблема 1: Auto-Deploy = No**
- Решение: Settings → Auto-Deploy → **Yes**

**Проблема 2: Webhook не настроен**
- Решение: Render → Settings → Disconnect/Reconnect Repository

**Проблема 3: Push идет в другую ветку**
- Решение: Проверьте, что пушите в `main`: `git branch`

**Проблема 4: GitHub webhook не срабатывает**
- Решение: GitHub → Repo Settings → Webhooks → найдите Render webhook → Recent Deliveries → проверьте ошибки

### Деплой падает

- Смотрите логи: Dashboard → Logs
- Проверьте, что код работает локально
- Проверьте `requirements.txt` - все зависимости установлены?

---

## ✅ Checklist для автодеплоя

- [ ] Auto-Deploy = **Yes** на Render
- [ ] Branch = **main** (или ваша ветка)
- [ ] Webhook есть в GitHub → Settings → Webhooks
- [ ] Тестовый push прошел успешно
- [ ] Уведомления настроены (опционально)

---

## 🚀 Готово!

Теперь каждый `git push` будет автоматически деплоить ваше приложение на Render!

**Следующий шаг:** Настройте переменные окружения (GARMIN_EMAIL, GARMIN_PASSWORD) и протестируйте синхронизацию с Garmin.

Полная документация: `HOW_IT_WORKS.md`
