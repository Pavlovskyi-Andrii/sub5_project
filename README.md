# Garmin Connect to Google Sheets Sync

Автоматическая синхронизация данных тренировок из Garmin Connect в Google Таблицы "ВЕЛ БЕГ".

## Возможности

- 📊 Автоматическое получение данных тренировок из Garmin Connect
- 🚴 Велосипедные тренировки: ваты, Normalized Power, TSS, скорость, каденс, ЧСС
- 🏃 Беговые тренировки: время, расстояние, темп, ЧСС, HRV
- 📈 Автоматическая запись в структурированную таблицу по неделям
- 🔒 Безопасное хранение учетных данных
- ⚡ Оптимизация API запросов через batch operations
- 🔍 Диагностическая выгрузка всех данных на лист "исходник"
- ⏰ Scheduled deployment для автоматической синхронизации по расписанию

## Структура таблицы "ВЕЛ БЕГ"

Таблица имеет фиксированную структуру с блоками тренировок по дням недели:

### Суббота (строки 7-15) - Вел длинная + бег брик
- **Строки 7-11**: метрики велосипеда (через "/" если 2 тренировки)
  - Средние ваты
  - Normalized Power
  - Средняя скорость (км/ч)
  - Частота вращения (каденс)
  - Средняя ЧСС
- **Строки 13-15**: метрики бега брик
  - Расстояние (км)
  - Темп (мин/км)
  - ЧСС

### Воскресенье (строка 20+) - Лонг RUN
- Время, расстояние, темп, ЧСС

### Понедельник (строка 33+) - Становая + плавание
- Длительность тренировки

### Вторник (строка 38+) - Короткие интервалы BIKE
- Время, расстояние, скорость, каденс

### Среда - Интервалы бег
- Время, расстояние, темп, ЧСС

### Пятница (строка 73+) - Отдых или сухое плавание

## Настройка

### 1. Создайте Service Account в Google Cloud

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Sheets API
4. Создайте Service Account:
   - Перейдите в "IAM & Admin" → "Service Accounts"
   - Нажмите "Create Service Account"
   - Дайте имя и создайте
   - Нажмите на созданный аккаунт → "Keys" → "Add Key" → "Create New Key" → JSON
   - Скачайте JSON файл

### 2. Настройте Google Таблицу

1. Откройте вашу таблицу "ВЕЛ БЕГ"
2. Скопируйте URL таблицы
3. Откройте JSON файл service account и найдите email (например: `xxx@xxx.iam.gserviceaccount.com`)
4. Поделитесь таблицей с этим email (дайте права на редактирование)

### 3. Настройте переменные окружения в Replit

Добавьте следующие секреты в Replit Secrets:

- `GARMIN_EMAIL` - ваш email от Garmin Connect
- `GARMIN_PASSWORD` - ваш пароль от Garmin Connect
- `GOOGLE_SHEET_URL` - URL вашей Google Таблицы
- `SERVICE_ACCOUNT_JSON` - содержимое JSON файла service account (весь текст)
- `DAYS_TO_SYNC` (опционально) - количество дней для синхронизации (по умолчанию 7)

### 4. Запустите скрипт

Скрипт можно запустить несколькими способами:

**Локально в Replit:**
```bash
python main.py
```

**Автоматически через Workflow:**
- Скрипт запускается автоматически через настроенный Workflow "Garmin Sync"

**Scheduled Deployment (рекомендуется для продакшена):**
- В Replit нажмите кнопку "Deploy"
- Выберите "Scheduled deployment"
- Настройте расписание (например: "Every day at 6 AM" или "Every Monday at 10 AM")
- Скрипт будет автоматически выполняться по расписанию

## Особенности работы

### Автоматическое определение недель
- Скрипт читает даты из строк блоков (20, 33, 38, 73) вместо заголовка
- Каждая неделя начинается с субботы
- Тренировки автоматически попадают в правильный столбец

### Агрегация данных
- Если в один день несколько велотренировок - метрики записываются через "/" (например: 164/146)
- Для бега всегда берется первая тренировка дня

### Форматирование
- Все данные записываются БЕЗ единиц измерения (только числа)
- Темп бега: "5:07" вместо "5:07 /км"
- Расстояние: "23.31" вместо "23.31 км"
- ЧСС: "159" вместо "159 уд./мин"

### Диагностика
Для отладки можно раскомментировать в `main.py` функцию `export_all_data_to_source()`:

```python
# Раскомментируйте эту строку для выгрузки всех данных
export_all_data_to_source(garmin, sheet)
```

Это создаст лист "исходник" со всеми доступными данными Garmin за период синхронизации.

## Устранение проблем

### Ошибка "401 Unauthorized" при авторизации Garmin

1. **Проверьте учетные данные:**
   - Убедитесь, что `GARMIN_EMAIL` и `GARMIN_PASSWORD` указаны правильно
   - Попробуйте войти на [connect.garmin.com](https://connect.garmin.com) в браузере

2. **Двухфакторная аутентификация (2FA):**
   - Если у вас включена 2FA, используйте сохраненную сессию (см. ниже)

3. **Используйте сохраненную сессию (рекомендуется):**
   - После первого успешного входа скрипт выведет `SESSION_SECRET`
   - Скопируйте это значение и добавьте его в Replit Secrets как `SESSION_SECRET`
   - Это позволит избежать повторной авторизации

### Проблемы с Google Sheets

1. Убедитесь, что вы поделились таблицей с email из Service Account
2. Проверьте, что `SERVICE_ACCOUNT_JSON` содержит полный JSON
3. Убедитесь, что Google Sheets API включен в Google Cloud Console
4. Если превышена квота API - подождите несколько минут (скрипт использует batch_update для оптимизации)

## Примечания

- Скрипт синхронизирует ВСЕ недели за `DAYS_TO_SYNC` дней (по умолчанию 7)
- Первый запуск требует авторизации в Garmin, последующие используют сохраненную сессию
- BatchUpdater класс оптимизирует API запросы к Google Sheets
- Поддержка всех типов тренировок: велосипед, бег, силовая, плавание
- Даты парсятся из строк блоков (20, 33, 38, 73) через batch_get запрос

---

## Web UI Dashboard

В проекте также есть веб-интерфейс для визуализации тренировочных данных.

### Возможности Web UI

- Интерактивный дашборд с графиками
- Просмотр всех активностей из локальной БД
- Еженедельная статистика
- Синхронизация данных из Garmin через веб-интерфейс
- Экспорт данных (JSON, CSV)
- Логи синхронизации

### Быстрый старт Web UI

1. **Установите зависимости:**
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

2. **Запустите приложение:**
```bash
python3 app.py
```

3. **Откройте браузер:**
```
http://localhost:5000
```

### Подробная документация

Полную документацию по Web UI см. в файле [START UP.md](START%20UP.md)
# sub5_project
